/**
 * MultiCategoryFilter Widget Admin JS library
 */
 
jQuery(function() {
	
	/*
	jQuery('td.column-cache-timeout input').change(function() {
		
		console.log('original value = ' + this.getAttribute('data-value') + ', changed value = ' + this.value ); 
		
		if(this.getAttribute('data-value') != this.value) {
			jQuery('#form-main select[name=action]').val('update_settings');
		}
		
	});*/
	
	//jQuery('#form-main').submit(function() {
		/*console.log('submitted');
		var bReturn = false;
		/*
		jQuery('#form-main td.column-cache-timeout input').each(function() {
			console.log(this.getAttribute('name') + ': ' + 'original value = ' + this.getAttribute('data-value') + ', changed value = ' + this.value )
		});*/
		
		/*.each(function() {
			
		
			console.log('original value = ' + this.getAttribute('data-value') + ', changed value = ' + this.value ); 
			if(this.getAttribute('data-value') != this.value) {
				jQuery('#form-main select[name=action]').val('update_settings');
				
				console.log(jQuery('#form-main select[name=action]').val());
				if(jQuery('#form-main select[name=action]:selected').val() == 'update_settings') {
					//bReturn = true;
				}
			}
		}*/
		
		
		//return bReturn;
	//});
	
	/*jQuery('.mcf-admin-refresh-category-tree').click(function() {
		console.log(this.getAttribute('class'))
		//widgetFormData
		
		jQuery.ajax({
			type: 'POST',
			url: acf_ajax.ajaxurl,
			data: widgetFormData,
			success: function( result ) {
		}
		})
	})*/
})
